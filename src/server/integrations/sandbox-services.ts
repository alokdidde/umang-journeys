// Compatibility export for older callers. External outcomes are produced only by
// external-agency-agent.ts through the Vercel AI SDK; this module contains no simulator.
export { isSandboxServiceKey } from "@/domain/service-workflows";
